{
  "routes": [
    {
      "method": "GET",
      "path": "/gears",
      "handler": "gear.find",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/gears/count",
      "handler": "gear.count",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/gears/:id",
      "handler": "gear.findOne",
      "config": {
        "policies": []
      }
    },
    {
      "method": "POST",
      "path": "/gears",
      "handler": "gear.create",
      "config": {
        "policies": []
      }
    },
    {
      "method": "PUT",
      "path": "/gears/:id",
      "handler": "gear.update",
      "config": {
        "policies": []
      }
    },
    {
      "method": "DELETE",
      "path": "/gears/:id",
      "handler": "gear.delete",
      "config": {
        "policies": []
      }
    }
  ]
}
